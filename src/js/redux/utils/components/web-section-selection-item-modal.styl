//Fullscreen
.modal
    background #eff2e7
    padding 0
.modal-close 
    padding 0
    
.modal-fullscreen.open{
    width: 100%;
    max-height: 100%;
    height: 100%;
    top: 0 !important;
    z-index:100000;
}

.modal-fullscreen > .modal-content {
  height: calc(100% - 56px);
}